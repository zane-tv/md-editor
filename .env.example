# Supabase config for share links
# The app stores shared markdown through Supabase RPC functions.
# Configure the remote schema to expose create_share(markdown, view_mode) and load_share(share_id).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
